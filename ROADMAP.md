# ROADMAP & Progress Tracker (Universal WebDAV & Network Drive Service)

Документ отслеживания этапов разработки универсального C# WebDAV-сервиса и сетевого диска Telegram.

Архивная дорожная карта версии V1 (AIMP Plugin) сохранена в файле: [`ROADMAP_V1_AIMP_PLUGIN.md`](./ROADMAP_V1_AIMP_PLUGIN.md).

---

## 📌 Текущий статус проекта
- **Фаза**: Реализованы все модули C# сервиса: база данных SQLite WAL, WebDAV сервер, WTelegramClient с защитой FLOOD_WAIT, парсер тегов ATL.NET, TrayContext и форма настроек WinForms.
- **Целевая платформа**: Windows Desktop / C# (.NET 8.0/9.0) Tray App & Background Service.
- **Протокол**: WebDAV (`http://localhost:{port}/`, настраиваемый порт) + Автомонтирование сетевого диска (`Z:`, `T:`, `Y:`, `W:` или авто-выбор).

---

## 📋 Этапы разработки

### [x] Этап 1: Проектирование и создание базы данных с нуля (`base.db`)
- [x] Разработка механизма инициализации чистой БД при первом запуске приложения.
- [x] Создание таблиц виртуальной файловой системы (узлы, папки, файлы, версии `v1`, `v2`, статусы `is_deleted`).
- [x] Создание таблицы трекинга незавершенных загрузок `upload_progress` (ID файла, позиция сбойного чанка, хеш) для докачки при обрывах.
- [x] Проектирование связей версионирования для файлов и структуры виртуальной корзины (`.Trash`).
- [x] Создание таблиц/колонок метаданных аудио (`artist`, `title`, `album`, `year`, `genre`, `track_number`, `duration_seconds`, `bitrate`).
- [x] Добавление поля хранения кэша первых 128 КБ заголовка файла (`header_cache_bytes`) и обложек альбомов.
- [x] Включение режима WAL для SQLite (`PRAGMA journal_mode=WAL;`) для безопасного конкурентного доступа.

### [x] Этап 2: Универсальный C# WebDAV Сервер
- [x] Создание легкого фонового HTTP/WebDAV сервера на C# (порт `37000`).
- [x] Создание структуры обработки запросов (OPTIONS, PROPFIND, GET, PUT, MKCOL, DELETE, MOVE).
- [x] Реализация XML-ответов `PROPFIND` с привязкой к SQLite.
- [x] Реализация записи, версионирования и управления: `PUT`, `MKCOL`, `DELETE`, `MOVE`.
- [x] Логика обработки перезаписи файлов: сохранение прежней версии как `v1` в корзину и присвоение текущему файлу версии `v2`.
- [x] Виртуальное отображение папки корзины (`Z:\.Trash` / `Z:\Корзина`) и механизм восстановления предыдущих версий. Исправлен баг отображения файлов в `.Trash` (добавлена умная выборка `is_deleted = 1` при чтении корзины).
- [x] Зеркалирование структуры каталогов в корзине: при удалении файлов или вложенных папок сохраняется их точный исходный путь внутри `.Trash` (например, `Z:\.Trash\Album\CD1\track.mp3`), предотвращая свалку файлов в корне корзины и потерю структуры папок при удалении через Проводник Windows.
- [x] Реализация полноценного перманентного удаления: при очистке корзины `.Trash` или удалении файлов из нее, данные сначала стираются из Telegram (включая рекурсивное пакетное удаление группами по 100 сообщений для папок с сотнями файлов для защиты от Flood Wait и таймаутов), а затем строки физически удаляются из SQLite базы данных.
- [x] Отказоустойчивое пакетное удаление из Telegram с бинарным делением (Биссекция): при возникновении ошибок типа `MESSAGE_ID_INVALID` (например, для файлов, удаленных ранее вручную в клиенте Telegram) пакет делится пополам, позволяя успешно и быстро удалить все валидные сообщения за минимальное число обращений к API.
- [x] Исправление первопричины появления дубликатов в корзине: универсальная обработка двухэтапного создания файлов Проводником Windows (0-байтовый плейсхолдер) и плагином WebDAV Total Commander (1-байтовый pre-flight probe). Файлы $\le 1$ байта больше не отправляются в Telegram впустую и не улетают в корзину при наполнении реальным содержимым, а обновляются на месте с версией 1. При необходимости 1 байт сохраняется локально в SQLite и отдается клиентам без обращения к Telegram.
- [x] Исправление бага восстановления/перемещения (`MoveNode`): корректный сброс флага `is_deleted = 0` при переносе из корзины и установка `is_deleted = 1` при переносе в корзину с рекурсивным обновлением вложенных элементов папок.
- [x] Реализация заголовков `Accept-Ranges: bytes` и статуса `206 Partial Content` для быстрой перемотки и докачки файлов на ПК.
- [x] Полноценная поддержка сохранения оригинальных временных меток файлов (Timestamps / LastModified / CreationDate):
  - Полноценная обработка метода `PROPPATCH`: извлечение свойств `<Win32LastModifiedTime>`, `<Win32CreationTime>`, `<getlastmodified>`, `<creationdate>` и сохранение реальных дат в базе SQLite (`updated_at`, `created_at`).
  - Поддержка заголовков `Last-Modified` и `X-OC-MTime` непосредственно в запросах `PUT` для установки точного времени файла с момента создания.
  - Добавление пространства имен `xmlns:Z="urn:schemas-microsoft-com:"` и тегов `<Z:Win32CreationTime>`, `<Z:Win32LastModifiedTime>` в ответы `PROPFIND`.
  - Устранение двойного сдвига часовых поясов (разницы в 3 часа): явная фиксация `DateTimeKind.Utc` при чтении из базы данных SQLite, исключающая повторную конвертацию локального времени при отдаче HTTP-заголовков.
  - Устранение ложных срабатываний и предупреждений «Папка назначения содержит более новые данные» в утилитах синхронизации (Allway Sync, FreeFileSync, GoodSync) и сохранение исторической даты изменения файлов при копировании через Проводник Windows и Total Commander секунда в секунду.
- [x] Автоматическое создание структуры родительских каталогов (On-Demand Parent Path Creation):
  - Метод `EnsureDirectoryPathExists` в `NodeRepository` для автоматического рекурсивного создания недостающих промежуточных папок (`mkdir -p`).
  - Бесшовная синхронизация каталогов в Total Commander: устранена ошибка «Ошибка при закачке файла!» (HTTP 409 Conflict), возникавшая из-за отправки файлов прямым `PUT` в несуществующие подпапки без предварительного `MKCOL`.
- [x] Автоматическое распознавание музыки «на лету» при безопасном копировании (Allway Sync / Atomic Save):
  - Анализ бинарных заголовков ID3v2 (MP3), FLAC, M4A, OGG прямо в потоке первых 128 КБ для временных файлов софта бэкапа (`.tmp`, `.temp`, `.part`).
  - Автоматическая подстановка боевого имени трека при отправке в Telegram MTProto: файлы превращаются в полноценные аудиозаписи встроенного плеера Telegram вместо «безымянных бинарников».
  - Единая строгая подпись (Caption) для всех файлов: формат `/Полный/Путь/Имя_vN.ext` (начиная с `_v1.ext` при создании и `_v2.ext` при перезаписи) для Проводника, Total Commander и Allway Sync.
  - Поддержка метода `MOVE`: при финализации переименования утилитой бэкапа сервер обновляет подпись (Caption) сообщения в Telegram через `TL.Methods.Messages_EditMessage` на актуальный полный путь с версией и обогащает базу данных SQLite недостающими тегами.
- [x] Оптимизация под Проводник Windows: полная реализация виртуальных Mock-блокировок (`LOCK`, `UNLOCK`) и обновления свойств (`PROPPATCH`) для 100% стабильной бесконфликтной совместимости с Проводником без пустых файлов и ошибок копирования.
- [x] Защита от прямой записи в корзину: строгая блокировка запросов `PUT` и `MKCOL` внутри каталога `/.Trash` со статусом `403 Forbidden` для предотвращения случайной загрузки файлов в Telegram и рассинхронизации состояния базы.
- [x] Защита системной корзины от случайного уничтожения (Вариант 1): блокировка удаления (`DELETE /.Trash`) и перемещения/переименования (`MOVE /.Trash`). При массовом удалении файлов в корне (Ctrl+A -> Delete) запрос на удаление самой корзины завершается статусом `204 NoContent` без уничтожения данных, позволяя клиентам (Проводник, Total Commander) безопасно переместить все пользовательские файлы в `.Trash`, сохранив их структуру и защитив от безвозвратного стирания в Telegram. Перманентное удаление файлов внутри корзины (`DELETE /.Trash/*`) сохраняет свою работоспособность.
- [x] Чистый корень диска и интеграция корзины в Windows Shell:
  - Скрытие `.Trash` из листинга корня диска при `PROPFIND` (опция `HideTrashFromRoot`, по умолчанию включена). Корень диска остаётся чистым, исключая случайное выделение корзины при Ctrl+A.
  - Нативная интеграция корзины в контекстное меню Проводника Windows (`ShellContextMenuHelper`): пункт «Открыть корзину WebDAV» с системной иконкой корзины (`shell32.dll,31`) с умным фильтром `AppliesTo` (пункт отображается исключительно при правом клике по сетевому диску или внутри его каталогов, не засоряя диски C:, D: и Рабочий стол; ветка `HKCU`, без требования прав администратора).
  - Пункт быстрого перехода «Открыть корзину (Z:\.Trash)» в меню системного трея приложения возле часов.
  - Настройки и переключатели «Скрывать папку .Trash из корня диска» и «Пункт "Открыть корзину WebDAV" в контекстном меню Windows» во вкладке «Сервер и Диск» окна настроек `AuthSettingsForm`.
- [x] Оптимизация потокового чтения и защита от скачивания при наведении курсора (InfoTip & Property Handlers):
  - Потоковый On-Demand стриминг из Telegram через `TeeStream`: одновременная отдача байт клиенту и сохранение в локальный дисковый кэш.
  - Адаптивный пейсинг первого блока (Initial Chunk Pacing): сервер мгновенно (за 0 мс) отдает первые 128 КБ заголовка клиенту, а затем делает короткую паузу (250 мс) для проверки активности сокета.
  - Мгновенное прерывание передачи: как только Проводник Windows считывает ID3-теги из первых 128 КБ для всплывающей подсказки (InfoTip) и закрывает дескриптор файла, сессия скачивания из Telegram немедленно прерывается. Файлы целиком (10–50 МБ) при наведении курсора не выкачиваются.
  - Бесшовное воспроизведение: при непрерывном воспроизведении в плеере (AIMP) или копировании первые 128 КБ дают более 3 секунд буфера звука, после чего трек продолжает скачиваться и кэшироваться на полной скорости без задержек.
  - Компактная архитектура SQLite: в базе данных хранятся только числовые и текстовые метаданные (название, артист, длительность и т.д.) без тяжелых бинарных BLOB-блоков, что сохраняет размер базы на уровне ~20 МБ даже для 60 000 треков.
  - Чистая файловая система: отсутствие искусственных файлов (`desktop.ini`), что сохраняет нативную поддержку любых режимов отображения папок Windows (включая вид «Музыка»).

### [x] Этап 3: Интеграция с WTelegramClient & Авторизация
- [x] Разработка каркаса `TelegramService` для подключения, управления сессией (`.session`) и многошаговой авторизации.
- [x] Реальная MTProto-авторизация через `WTelegram.Client` (отправка реального кода в Telegram-клиент, поддержка 2FA).
- [x] Устранение сбоя стейт-машины логина (`WTException: You must provide a config value for phone_number`): разделение проверки сохраненной сессии на диске и первичного входа по номеру телефона, защита от сбоев в `ConnectAsync()`.
- [x] Сохранение `PhoneNumber` в `appsettings.json` и передача в конфигуратор WTelegramClient: устранение фантомных запросов номера телефона при повторных запусках приложения.
- [x] Поля ввода `API ID` и `API Hash` в графическом интерфейсе (вкладка Telegram) с сохранением в `appsettings.json` без хардкода.
- [x] Встроенный интерфейс входа в Telegram (ввод номера телефона, получение SMS/Telegram кода, ввод 2FA-пароля).
- [x] Исправление верстки WinForms формы настроек: отключение паразитного переноса столбцов вправо (`WrapContents = false`), расширение окна до 570x520 px, исключение горизонтального скроллбара.
- [x] Управление файлом сессии `.session` и отслеживание статуса подключения (Подключено / Требуется вход).
- [x] Реализация двухнаправленного потока с поддержкой докачки:
  - Стриминг при чтении из Telegram (`Client.DownloadFileAsync`) по Range-запросам (`offset` + `count`).
  - Надежная загрузка чанков в Telegram (`Client.UploadFileAsync`) с возможностью возобновления оборванных загрузок (Resumable Uploads) по прогрессу из `base.db`.
  - Поддержка заголовка `Content-Range` в запросах `PUT` со статусом `202 Accepted` для умного софта резервного копирования (Rclone, GoodSync, FreeFileSync).
  - Автоматическая очистка временных записей из `upload_progress` при успешном завершении загрузки файла.
- [x] Отработка паузы потока при получении `FLOOD_WAIT` от Telegram без разрыва соединения с Проводником.
- [x] Буферизация памяти с помощью `ArrayPool<byte>`.
- [x] Разработка высокопроизводительного асинхронного файлового логгера (`AppLogger`) на базе `System.Threading.Channels` с ротацией до 5 МБ (`logs/app.log`, `app.1.log`) и перехватом логов `WTelegramClient`.

### [x] Этап 4: Автоматический парсинг метаданных (ATL.NET)
- [x] Создание `AudioMetadataExtractor.cs` с извлечением ID3v1/ID3v2/FLAC тегов.
- [x] Автоматическое извлечение ID3-тегов и обложек "на лету" при записи (`PUT`) в `WebDavMiddleware`.
- [x] Сохранение кэша первых 128 КБ заголовка файла (`header_cache_bytes`) для мгновенного ответа плееру AIMP.

### [x] Этап 5: Окно Настроек (Settings UI), Переключатели Режимов и Tray App
- [x] Разработка менеджера конфигурации (`appsettings.json`) для хранения настроек (api_id, порты, пути).
- [x] Защита от дубликатов (Single Instance Pattern):
  - Глобальный системный `Mutex` (`Global\TelegramWebDAV_SingleInstance_Mutex`) для предотвращения запуска второго экземпляра приложения.
  - Повторный клик по трею фокусирует уже открытое окно настроек (`BringToFront()` / `Activate()`), не плодя новых окон.
- [x] Окно конфигурации с переключателями:
  - `[x] Включить WebDAV сервер`
  - `[x] Драйвер диска: WinFsp (прямой стриминг в ОЗУ) / WebDAV`
  - `[x] Монтировать как сетевой диск`
- [x] Реализация нативного виртуального диска WinFsp (`WinFspServer.cs` + `VirtualDriveManager.cs`):
  - Полноценная поддержка FUSE для Windows через встроенные биндинги WinFsp (`TelegramWebDAV/Services/WinFsp/Native/WinFspNative.cs`), скомпилированные нативно под .NET 8.
  - Устранена первопричина критического падения статического конструктора WinFsp (`The path is empty` и `cannot load telegramwebdav-x64.dll`): полный отказ от устаревшего пакета `winfsp.net` и Mono.Cecil-патчей `WinFspFixer`, интеграция чистой безопасной инициализации P/Invoke без несовместимого с Single-File бандлом метода `CheckVersion` и с жесткой фиксацией нативного имени библиотеки `winfsp-x64.dll`.
  - Автоматическая регистрация `SetDllDirectory` и предзагрузка `winfsp-x64.dll` через `NativeLibrary.Load`.
  - Прямое точечное чтение чанков MTProto прямо в оперативную память плееров и Проводника без промежуточной записи на системный диск `C:` (`TfsStore\Tfs_DAV`).
  - Устранена первопричина `LIMIT_INVALID` в MTProto: строгое выравнивание `chunkOffset` по размеру чанка, исключающее пересечение границ 1 МБ (1048576 байт).
  - Полноценный цикл файловых операций в `TelegramWinFspFileSystem`: создание папок (`Create` + `FILE_DIRECTORY_FILE`), загрузка файлов в Telegram при копировании (`Create`, `Write`, `Cleanup`), переименование (`Rename`), удаление (`CanDelete`, `SetDelete`, `CleanupDelete`) и локальный дескриптор безопасности без ложных предупреждений Windows SmartScreen.
  - Динамический размер чанка MTProto (512 КБ для потокового чтения, 128 КБ для тегов) и корректное информирование трея с реальным размером файлов.
  - Двухуровневое кэширование в оперативной памяти: кэширование дескрипторов документов Telegram (`TL.Document`) с автообновлением по TTL и кольцевой кэш чанков MTProto (~8 МБ в ОЗУ), устранившие лишние RPC-запросы `Channels_GetMessages` и обеспечившие мгновенный старт воспроизведения в AIMP без задержек.
  - Сохранение `StorageChannelAccessHash` в конфигурацию `AppSettings.json` и фоновый прогрев канала сразу при авторизации: устранена 1.5-секундная задержка при первой загрузке/чтении файла, прямое подключение канала-хранилища за 0 мс с автоматическим самовосстановлением при ошибках `CHANNEL_INVALID`.
  - Адаптивные MTProto-чанки и упреждающее фоновое чтение (Read-Ahead Prefetch):
    - Смещение 0 для тегов: ровно 256 КБ (1 запрос вместо 2 для ID3v2 и обложки).
    - Хвост файла: 128 КБ (для ID3v1).
    - Потоковое аудио: 1 МБ максимальными чанками с параллельной фоновой подгрузкой следующих 2 блоков (Multi-Chunk Prefetch на 2 МБ вперед) и немедленным прогревом первых мегабайт, сократившие время старта треков до минимума без записи на диск.
    - Устранена первопричина мерцания и спама таймеров оверлея трея: вызов `OnDownloadCompleted` скорректирован строго на завершение передачи всего файла (`currentPos >= actualTotalSize`).
    - **Очередь последовательной загрузки файлов (Upload Queue / Semaphore):** устранена параллельная конкуренция за канал при одновременном копировании нескольких файлов, предотвращены ошибки `FLOOD_WAIT` и мерцание UI оверлея, добавлено отображение счетчика файлов в очереди в заголовке окна передачи `(в очереди: N)`.
    - **Оптимизация потокового скачивания из Telegram (Stream Prefetching Pipeline):** устранена первопричина «зубьев пилы» на графике сети и просадок скорости до 2–3 МБ/с. Внедрен непрерывный конвейер упреждающего чтения на 3 МБ вперед (`currentBlock + 1MB`, `+ 2MB`, `+ 3MB`), размер кольцевого кэша ОЗУ увеличен до 64 чанков с интеллектуальным LRU-вытеснением.
    - **Семафор параллельности MTProto (`_downloadRpcSemaphore`) и защита от FLOOD_WAIT:** ограничение одновременных RPC-запросов на скачивание чанков строго до 2 параллельных потоков с обязательным соблюдением пауз `_floodWaitUntil` в префетче, предотвращающее ошибки `FLOOD_WAIT_2` при интенсивном чтении больших файлов.
    - **Гарантированное закрытие оверлея передачи:** исправлено условие `OnDownloadCompleted`, устранено зависание всплывающего окна прогресса при завершении чтения файла Проводником.
    - **Устранение ошибки доступа WinFsp и «Неверная функция» (Universal SDDL `O:WDG:WDD:P(A;;FA;;;WD)`):** внедрен универсальный дескриптор безопасности полного доступа для всех пользователей (`World/Everyone`), реализован `GetSecurityByName`, а в `WindowsRegistryFixer` добавлена регистрация `EnableLinkedConnections = 1` для сквозной видимости диска как в обычных программах, так и в запущенных от имени Администратора.
    - **Настраиваемый объём диска и динамическое авто-расширение:** настраиваемый базовый размер диска в ГБ (по умолчанию 1024 ГБ / 1 ТБ) в окне настроек, подсчет реального занятого места из SQLite (`NodeRepository.GetTotalUsedSpaceBytes`), опция учета корзины `.Trash` и интеллектуальное авто-расширение ёмкости при заполнении > 70% (диск в Проводнике всегда имеет >= 30% свободного места и не окрашивается в красный цвет).
  - Устранена первопричина сбоев и тормозов службы Windows WebClient.
  - Автоматическое переключение на WebDAV в случае отсутствия установленного драйвера WinFsp.
  - Выбор выбранного движка в окне «Настройки подключения» (`AuthSettingsForm.cs`).
- [x] Настройки WebDAV: изменение порта (по умолчанию `37000`), копирование готового URL.
- [x] Настройки Сетевого Диска: выбор буквы (`Z:`, `T:`, `Y:`, `W:`, Auto), выбор имени тома.
- [x] **Кнопка-фикс реестра с авто-запросом UAC**: "Применить комплексный фикс реестра (с запросом UAC)" — автоматический вызов стандартного окна повышения прав Windows (UAC) без необходимости ручного перезапуска всей программы от Администратора; применение `BasicAuthLevel=2` (разрешение HTTP), `FileSizeLimitInBytes=4GB` (снятие лимита 50 МБ), `EnableLinkedConnections=1` (доступность диска от Администратора) и настройка `ZoneMap` (подавление предупреждений безопасности при копировании файлов).
- [x] Кнопка-индикатор "Авторизация": статус сессии, пошаговый мастер (Телефон -> Код -> 2FA) и карточка профиля.
- [x] Интерактивный проводник файлов и папок с отображением версий (`_v1`, `_v2`), корзины `.Trash` и аудиотегов.
- [x] Системный трей Windows (Tray App):
  - Информативная шапка статуса авторизации в трей-меню с кастомным рендерером: префикс «Статус: » обычным контрастным текстом (без серости неактивного контрола), статус «Авторизовано ✔» (зеленый полужирный шрифт `ForestGreen`) или «Не авторизовано ❌» (красный полужирный шрифт `Firebrick`).
  - Горизонтальная разделительная линия под шапкой статуса и динамическое обновление состояния при открытии меню (`Opening`).
- [x] Инспектор аудио-тегов с симулятором AIMP HTTP 206 Seek и кэша заголовков.
- [x] Журнал запросов WebDAV (PROPFIND, GET, PUT, MKCOL, DELETE, MOVE) в реальном времени.

### [x] Этап 6: Непрерывная интеграция и релизы (GitHub Actions)
- [x] Создан файл рабочего процесса `.github/workflows/release.yml`.
- [x] Автоматический инкремент версии `1.0.${{ github.run_number }}` (+1 на каждый пуш).
- [x] Публикация в режиме **Self-Contained Single File** под Windows x64 (не требует установки .NET Runtime).
- [x] Упаковка в ZIP и загрузка исполняемого файла `.exe`.
- [x] Автоматическое создание GitHub Release с тегом `v1.0.${{ github.run_number }}`.
- [x] Полное устранение предупреждений компилятора C# (CS8600–CS8625, SYSLIB0013) с приведением всех моделей и репозиториев к строгой Null-безопасности (`0 Warning(s), 0 Error(s)`).

### [ ] Этап 7: Устранение заглушек (Mocks) и переход на полноценные протоколы
- [x] **7.1. Реальная отправка файлов в Telegram в отдельный приватный канал (`TelegramService.UploadFileAsync` / `UploadFileChunkAsync`)**:
  - Замена мока со случайным ID (`Random.Next`) на реальную отправку в приватный канал через `_client.UploadFileAsync(stream)` и `_client.SendMediaAsync(...)`.
  - Автоматическое создание приватного канала при отсутствии ID в конфиге (`StorageChannelId == 0`) с настраиваемым именем (по умолчанию `"Telegram WebDAV Drive"`).
  - Никакого использования «Избранного» (Saved Messages) и никакого неявного поиска среди чужих диалогов — строгая изоляция хранилища.
  - Поле редактирования имени канала-хранилища в окне настроек (вкладка Telegram) с сохранением в `appsettings.json`.
  - Потоковая сборка и отправка чанков с поддержкой возобновляемой загрузки (Resumable Uploads).
  - Получение настоящего `Message.ID` из Telegram и привязка к ноде в `base.db`.
  - **Сквозной потоковый пайплайн (Direct Streaming & TCP Flow Control)**: устранена промежуточная буферизация на диск при штатной загрузке файлов (`StreamingUploadStream`). Входящий сетевой поток WebDAV передается напрямую в MTProto-клиент Telegram порциями по 512 КБ.
  - **Живой HUD-виджет прогресса в системном трее (`TrayProgressOverlay`)**: всплывающее без перехвата фокуса (`WS_EX_NOACTIVATE`) окно карточки в правом нижнем углу над панелью задач с градиентным прогресс-баром, точным объемом (`X из Y МБ`), процентом и сглаженной скоростью отдачи (`МБ/с`). Карточка появляется автоматически при начале отправки и обновляется в реальном времени каждые 200–250 мс.
  - Настраиваемый режим показа: в окне настроек добавлена опция «Автоматически открывать карточку прогресса при отправке файлов» (при отключении карточка открывается только при ручном наведении мыши на иконку в трее).
  - Оптимизация извлечения аудиотегов: парсер `AudioMetadataExtractor` считывает только первые 128 КБ заголовка в ОЗУ и сразу объединяет их в потоковый адаптер без записи гигабайтных аудиофайлов на жесткий диск.
- [x] **7.2. Реальное скачивание и точечный стриминг чанков MTProto (`TelegramService.DownloadFileAsync`)**:
  - Реализация честных точечных запросов чанков через нативный метод MTProto `Upload_GetFile` с параметром `precise: true`.
  - Устранена первопричина нежелательного выкачивания файлов при наведении курсора мыши и фоновой индексации AIMP: при запросе первых 128 КБ заголовка (Range 0-131071) у Telegram запрашивается ровно 1 чанк 128 КБ, без вызова `DownloadFileAsync` и без фонового скачивания оставшейся части файла (10–100 МБ).
  - Поддержка произвольного смещения (`offset`) и длины (`length`) для мгновенного Seek в плеере и чтения концевых тегов (ID3v1).
  - Автоматическая маршрутизация запросов к нужному дата-центру Telegram (`GetClientForDC` при несовпадении DC документа или ошибке `FILE_MIGRATE_X`).
  - Умная обработка закрытия соединения клиентом (Проводник / плеер прочитал теги и закрыл сокет — цикл немедленно прерывается без лишних сетевых затрат).
  - Отключение паразитных всплывающих окон прогресса в системном трее при быстром чтении метаданных ($\le 256$ КБ).
- [x] **7.3. Полноценная реализация HTTP HEAD в WebDAV (`WebDavMiddleware.HandleHeadAsync`)**:
  - Замена возврата `404 Not Found` на поиск запрашиваемой ноды по пути в SQLite (`NodeRepository`).
  - Корректная отдача мета-заголовков: `Content-Length`, `Content-Type`, `Accept-Ranges: bytes`, `Last-Modified` без тела ответа, что позволяет плеерам (AIMP) и Проводнику корректно определять поддержку Range и атрибуты файла перед воспроизведением.
- [ ] **7.4. Полноценный бинарный парсинг аудио-метаданных (`AudioMetadataExtractor.cs`)**:
  - Замена фиктивных значений (320 kbps, 210 сек) на полноценное чтение фреймов ID3v2 (`TIT2`, `TPE1`, `TALB`, `TLEN`) и заголовков MPEG Audio frame для точного расчета длительности и битрейта.

### [ ] Этап 8: Интеграционное тестирование
- [ ] Проверка работы с AIMP («Мои облака» и проигрывание с диска `Z:\`).
- [ ] Проверка работы с Total Commander и Проводником Windows.
- [ ] Проверка работы с утилитами бэкапа и синхронизации.

### [ ] Этап 9 (Перспектива / R&D): Нативный виртуальный диск через WinFsp
- [ ] Исследование интеграции библиотеки `WinFsp.NET` для создания полноценного виртуального диска (FUSE для Windows).
- [ ] Преимущества: монтирование без ограничений реестра (`BasicAuthLevel`, `FileSizeLimit`), отсутствие лимита 50 МБ в Проводнике, прямая интеграция с Windows Cache Manager в RAM.
- [ ] Архитектура гибридного режима: WebDAV остаётся основным протоколом для внешних плееров (AIMP, мобильные устройства), а WinFsp подключается опционально при обнаружении установленного драйвера в системе.
- [ ] Анализ компромисса: необходимость инсталлятора драйвера ядра (`WinFsp.msi`) против 100% портативности текущего WebDAV решения.
